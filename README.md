# Tasks

A macOS app for managing Google Tasks, built with SwiftUI.

## Google Sign-In

If you fork this project, replace the `GIDClientID` and the `CFBundleURLSchemes` associated with the `GoogleSignIn` in `Info.plist` with credentials from your own [Google Cloud Console](https://console.cloud.google.com) OAuth client.
